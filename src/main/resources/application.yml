server:
  port: 8080

spring:

  application:
    name: @project.name@
    version: @project.version@

  mvc.favicon.enabled: false

  profiles:
    include:
      - application-qa
      - application-prod

  main:
    banner-mode: CONSOLE